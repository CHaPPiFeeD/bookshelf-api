export type SignInResType = {
  accessToken: string,
  refreshToken: string,
}

export type InviteTokenType = {
  email: string,
}

export type RefreshTokenResType = {
  accessToken: string,
  refreshToken: string,
}
